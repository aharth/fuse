for e in ./feature-extraction/*.mtx; do for height in {0.25,0.5,0.75}; do Rscript ./clustering/clusterScript.R ${e} ${e%_*.mtx}.nx ./clustering/$(basename ${e%.mtx})_hclust_height_${height}_single.clu $height hclust_height single ./clustering/$(basename ${e%.mtx})_hclust_height_${height}_single.svg; done; done
for e in ./feature-extraction/*.mtx; do for height in {0.25,0.5,0.75}; do Rscript ./clustering/clusterScript.R ${e} ${e%_*.mtx}.nx ./clustering/$(basename ${e%.mtx})_hclust_height_${height}_complete.clu $height hclust_height complete ./clustering/$(basename ${e%.mtx})_hclust_height_${height}_complete.svg; done; done
for e in ./feature-extraction/*.mtx; do for height in {0.25,0.5,0.75}; do Rscript ./clustering/clusterScript.R ${e} ${e%_*.mtx}.nx ./clustering/$(basename ${e%.mtx})_hclust_height_${height}_average.clu $height hclust_height average ./clustering/$(basename ${e%.mtx})_hclust_height_${height}_average.svg; done; done
